import { useCallback, useEffect, useState } from 'react'
import { ArrowsClockwiseIcon, PlugsConnectedIcon, PlusIcon, XIcon } from '@phosphor-icons/react'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { apiRequest } from '@/lib/api'
import { formatDate } from '@/lib/formatters'
import { getErrorMessage, notify } from '@/lib/notifications'
import { hasPermission } from '@/lib/usersRoles'
import { CrudModal, type CrudField } from './CrudModal'
import { TableActions } from './TableActions'

type Router = Record<string, any> & { public_id: string; name: string }
type Result = { status: 'connected' | 'not_configured' | 'unsupported' | 'failed'; message?: string; checked_at?: string; driver?: string }

const fields: CrudField[] = [
  { name: 'name', label: 'Name', required: true }, { name: 'vendor', label: 'Vendor', required: true, searchable: false, options: [['mikrotik', 'MikroTik'], ['juniper', 'Juniper'], ['cisco', 'Cisco'], ['linux_frr', 'Linux / FRR'], ['other', 'Other']].map(([value, label]) => ({ value, label })) },
  { name: 'hostname', label: 'Hostname' }, { name: 'management_ip', label: 'Management IP' }, { name: 'model', label: 'Model' }, { name: 'software_version', label: 'Software version' }, { name: 'serial_number', label: 'Serial number' },
  { name: 'driver', label: 'Driver', required: true, searchable: false, options: [['mikrotik_router', 'MikroTik router'], ['juniper_router', 'Juniper router'], ['cisco_router', 'Cisco router'], ['linux_frr_router', 'Linux / FRR router']].map(([value, label]) => ({ value, label })) },
  { name: 'preferred_transport', label: 'Preferred transport', required: true, searchable: false, options: ['api', 'ssh', 'netconf', 'snmp', 'mock'].map(value => ({ value, label: value.toUpperCase() })) },
  { name: 'status', label: 'Status', required: true, searchable: false, options: ['active', 'inactive', 'maintenance', 'unknown'].map(value => ({ value, label: value[0].toUpperCase() + value.slice(1) })) },
  { name: 'notes', label: 'Notes', type: 'textarea' },
]

function values(row?: Router) { return row ? Object.fromEntries(fields.map(field => [field.name, String(row[field.name] ?? '')])) : { preferred_transport: 'mock', status: 'unknown', driver: 'mikrotik_router', vendor: 'mikrotik' } }
function label(value: string | undefined) { return value ? value.replaceAll('_', ' ').replace(/\b\w/g, letter => letter.toUpperCase()) : '—' }
function endpoint(row: Router) { return row.management_ip || row.hostname || 'Not configured' }
function stateBadge(status: string) { return <Badge variant={status === 'active' || status === 'connected' ? 'default' : status === 'failed' ? 'destructive' : 'secondary'}>{label(status)}</Badge> }

export function RoutersPage({ token, permissions, isSuperadmin }: { token: string; permissions?: string[]; isSuperadmin?: boolean }) {
  const can = (permission: string) => isSuperadmin === true || hasPermission(permissions, permission)
  const [rows, setRows] = useState<Router[]>([]); const [page, setPage] = useState(1); const [lastPage, setLastPage] = useState(1)
  const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [modal, setModal] = useState<'create' | 'edit' | 'view' | null>(null); const [selected, setSelected] = useState<Router | undefined>()
  const [modalError, setModalError] = useState(''); const [saving, setSaving] = useState(false); const [testing, setTesting] = useState(false); const [result, setResult] = useState<Result | null>(null)
  const load = useCallback(async () => { setLoading(true); setError(''); try { const response = await apiRequest<{ data: { data: Router[]; last_page?: number } }>(`/routers?per_page=20&page=${page}`, {}, token); setRows(response.data.data); setLastPage(response.data.last_page || 1) } catch (exception) { setError(getErrorMessage(exception, 'Unable to load routers.')) } finally { setLoading(false) } }, [page, token])
  useEffect(() => { void load() }, [load])
  const close = () => { setModal(null); setSelected(undefined); setModalError(''); setResult(null) }
  const save = async (form: Record<string, string>) => { setSaving(true); setModalError(''); try { await notify.promise(apiRequest(modal === 'create' ? '/routers' : `/routers/${selected?.public_id}`, { method: modal === 'create' ? 'POST' : 'PUT', body: JSON.stringify(form) }, token), { loading: modal === 'create' ? 'Creating router…' : 'Saving router…', success: modal === 'create' ? 'Router created.' : 'Router updated.', error: 'Unable to save router.' }); close(); await load() } catch (exception) { setModalError(getErrorMessage(exception, 'Unable to save router.')) } finally { setSaving(false) } }
  const archive = async (row: Router) => { try { await notify.promise(apiRequest(`/routers/${row.public_id}`, { method: 'DELETE' }, token), { loading: 'Archiving router…', success: 'Router archived.', error: 'Unable to archive router.' }); await load() } catch (exception) { setError(getErrorMessage(exception, 'Unable to archive router.')) } }
  const test = async (row: Router) => { setSelected(row); setModal('view'); setTesting(true); setResult(null); try { const response = await apiRequest<{ data: Result }>(`/routers/${row.public_id}/connection-test`, { method: 'POST' }, token); setResult(response.data) } catch (exception) { setResult({ status: 'failed', message: getErrorMessage(exception, 'The connection test failed.') }); notify.error('Unable to test router connection.') } finally { setTesting(false) } }
  const view = async (row: Router) => { setSelected(row); setModal('view'); setResult(null); try { const response = await apiRequest<{ data: Router }>(`/routers/${row.public_id}`, {}, token); setSelected(response.data) } catch { /* list data remains useful if detail loading fails */ } }
  const viewFields = selected ? [['Vendor', label(selected.vendor)], ['Driver', selected.driver], ['Hostname', selected.hostname], ['Management IP', selected.management_ip], ['Model', selected.model], ['Software version', selected.software_version], ['Serial number', selected.serial_number], ['Preferred transport', label(selected.preferred_transport)], ['Status', label(selected.status)], ['Last contact', selected.last_contact_at ? formatDate(selected.last_contact_at) : 'Never contacted'], ['Last synchronized', selected.last_synchronized_at ? formatDate(selected.last_synchronized_at) : 'Never synchronized']] : []
  return <div className="flex flex-col gap-5"><div className="billing-page-heading flex items-center justify-between gap-4"><div><p className="billing-modal-eyebrow">ISP BILLING / NETWORK</p><h1 className="text-sm font-semibold">Routers</h1><p className="mt-1 text-xs text-muted-foreground">Manage vendor-neutral router inventory and connection capabilities.</p></div>{can('routers.create') && <Button onClick={() => { setSelected(undefined); setModal('create') }}><PlusIcon data-icon="inline-start" />New router</Button>}</div>{error && <Alert variant="destructive"><AlertTitle>Could not load routers</AlertTitle><AlertDescription>{error}</AlertDescription></Alert>}<div className="billing-records overflow-x-auto rounded-lg border bg-card"><Table className="min-w-[1080px]"><TableHeader><TableRow>{['Router', 'Vendor / model', 'Management endpoint', 'Driver / transport', 'Status', 'Last contact', 'Actions'].map(column => <TableHead key={column}>{column}</TableHead>)}</TableRow></TableHeader><TableBody>{loading ? <TableRow><TableCell colSpan={7} className="h-48 text-center">Loading routers…</TableCell></TableRow> : rows.length === 0 ? <TableRow><TableCell colSpan={7} className="h-48 text-center">No routers found.</TableCell></TableRow> : rows.map(row => <TableRow key={row.public_id}><TableCell><p className="font-medium">{row.name}</p><p className="font-mono text-[10px] text-muted-foreground">{row.hostname || row.management_ip || '—'}</p></TableCell><TableCell><p>{label(row.vendor)}</p><p className="text-xs text-muted-foreground">{row.model || '—'}</p></TableCell><TableCell>{endpoint(row)}</TableCell><TableCell><p className="font-mono text-xs">{row.driver}</p><p className="text-xs text-muted-foreground">{label(row.preferred_transport)}</p></TableCell><TableCell>{stateBadge(row.status)}</TableCell><TableCell>{row.last_contact_at ? formatDate(row.last_contact_at) : 'Never contacted'}</TableCell><TableCell><div className="inline-flex items-center justify-end gap-1"><TableActions label={row.name} kind="operational" onView={() => void view(row)} onEdit={can('routers.update') ? () => { setSelected(row); setModal('edit') } : undefined} onArchive={can('routers.delete') ? () => void archive(row) : undefined} />{can('routers.test') && <Button type="button" variant="ghost" size="icon-sm" aria-label={`Test connection for ${row.name}`} title="Test connection" onClick={() => void test(row)} disabled={testing}><PlugsConnectedIcon /></Button>}</div></TableCell></TableRow>)}</TableBody></Table></div>{lastPage > 1 && <div className="flex items-center justify-end gap-2"><Button variant="outline" size="sm" disabled={page === 1} onClick={() => setPage(value => value - 1)}>Previous</Button><span className="text-xs text-muted-foreground">Page {page} of {lastPage}</span><Button variant="outline" size="sm" disabled={page === lastPage} onClick={() => setPage(value => value + 1)}>Next</Button></div>}<CrudModal open={modal === 'create' || modal === 'edit'} mode={modal === 'create' ? 'create' : 'edit'} title={modal === 'create' ? 'New router' : `Edit ${selected?.name || 'router'}`} description="Manage router inventory without storing credentials." fields={fields} initialValues={values(selected)} error={modalError} loading={saving} onClose={close} onSubmit={save} wide />{modal === 'view' && selected && <div className="billing-modal-backdrop fixed inset-0 z-50 grid place-items-center overflow-y-auto p-4" role="dialog" aria-modal="true" aria-labelledby="router-view-title"><Card className="billing-modal-card relative w-full max-w-3xl shadow-2xl"><CardHeader className="border-b pr-14"><p className="billing-modal-eyebrow">RECORD DETAILS</p><CardTitle id="router-view-title">{selected.name}</CardTitle><CardDescription>Review router inventory and driver capabilities.</CardDescription><Button type="button" variant="ghost" size="icon-sm" className="absolute right-4 top-4" onClick={close} aria-label="Close modal"><XIcon /></Button></CardHeader><CardContent className="space-y-5 pt-5"><dl className="grid gap-4 sm:grid-cols-2">{viewFields.map(([key, value]) => <div key={key}><dt className="font-mono text-[10px] uppercase tracking-[0.14em] text-muted-foreground">{key}</dt><dd className="mt-1 text-sm">{value || '—'}</dd></div>)}</dl><section className="rounded-md border"><div className="border-b px-4 py-3"><h3 className="text-sm font-semibold">Capabilities</h3></div>{selected.capabilities && Object.keys(selected.capabilities).length ? Object.entries(selected.capabilities).map(([key, value]) => <div className="flex items-center justify-between gap-3 border-b px-4 py-3 last:border-0" key={key}><span className="text-sm">{label(key)}</span>{stateBadge(String(value))}</div>) : <p className="px-4 py-4 text-sm text-muted-foreground">No capabilities reported.</p>}</section><div className="rounded-md border bg-muted/30 p-4"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold">Connection test</p><p className="mt-1 text-xs text-muted-foreground">{result?.message || 'Verify whether the configured driver can reach this router.'}</p></div>{can('routers.test') && <Button type="button" size="sm" onClick={() => void test(selected)} disabled={testing}>{testing && <ArrowsClockwiseIcon className="animate-spin" data-icon="inline-start" />}{testing ? 'Testing…' : result ? label(result.status) : 'Test connection'}</Button>}</div>{result && <div className="mt-3 flex items-center gap-3 text-xs">{stateBadge(result.status)}{result.checked_at && <span>Tested at {formatDate(result.checked_at)}</span>}</div>}</div></CardContent></Card></div>}</div>
}
